---
applyTo: "**/*.{ts,tsx,css}"
---

# Styling mandatory rules
- Tailwind CSS is the primary styling system.
- Keep classnames readable; wrap lines if long.
- Prefer reusable components for repeated patterns.
- Global CSS should be minimal and live in `src/styles`.
